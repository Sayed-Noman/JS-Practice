// Example: A variable that holds `undefined` value to represent a state
let userReward: undefined;

userReward = undefined;  // Correct assignment
console.log(userReward);  // undefined

// If we tried to assign anything else, it would cause an error
// userReward = 25; // Error: Type 'number' is not assignable to type 'undefined'.
